import java.util.ArrayList;

public class BSTester { //I am going to clean this up

    String testing = "T E S T I N G";
            String[] key = testing.split(" ");
            BSTNode the = new BSTNode();
            BST tree = new BST();

            ArrayList<String> stringTest = new ArrayList<String>(50);


            /*int n = key.length;
            for (int i = 0; i < n; i++)
                stringTest.get(i);
            for (String s: string)
                System.out.println(s + " " + string.get(s));
    */
}
